<div class="row clearfix">
	<%= render partial: "users/profile" if current_user? %>
	<div class="col-md-9 column">
		<%= render partial: "users/nav" if current_user? %>

		<div>
		        <a class="title">Veelgestelde vragen</a><br>
					<%= simple_format @page.faq if @page %>
		</div>
	</div>
</div>

